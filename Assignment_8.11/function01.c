#include<stdio.h>
int max(int a,int b)
{
    int maximum;
    if(a>b)
    {
        maximum = a;
    }
    else
    {
        maximum = b;
    }
    return maximum;
}

int main()
{
     int max, a = 500,b = 400;
     printf("max is: %d",maximum);
}
